-- // Linoria Lib \\
local repo = 'https://raw.githubusercontent.com/mstudio45/LinoriaLib/main/'
local Library = loadstring(game:HttpGet(repo .. 'Library.lua'))()
local ThemeManager = loadstring(game:HttpGet(repo .. 'addons/ThemeManager.lua'))()
local SaveManager = loadstring(game:HttpGet(repo .. 'addons/SaveManager.lua'))()
-- // Service [$] \\
local WCR = workspace.CurrentRooms
local Players = game.Players.LocalPlayer.Character.Humanoid
local CameraFOV = workspace.CurrentCamera.FieldOfView
local forCR = for _,v in pairs(WCR:GetDescendants())
-- // Nofitication [$] \\
local Wn = Library:Notify
-- // ESP Floor:Hotel \\
forCR do
if v:IsA("BasePart") and v.Name == "Door" and v.Parent.Name == "Door" then

